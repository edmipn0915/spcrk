# Sparck 灵愿 — 前后端分离计划书

> 项目路径：`e:\AI\spcrk`
> 项目许可证：Apache License 2.0
> 技术栈：Kotlin / Jetpack Compose / Room / OkHttp
> 文档版本：v1.1（2026-08-28）
> 实施进度：阶段 1~4（AppContainer + ViewModel 化）✅ 已完成并落地；第四章六层架构为下一阶段蓝图

---

## 一、架构审计总览

### 1.1 审计方法

| 审计范围 | 覆盖内容 |
|----------|----------|
| 全量源码 | `app/src/main/java/com/spcrk/app/` 全部 105 个文件 |
| 依赖审计 | `app/build.gradle` 全部直接依赖（AndroidX / OkHttp / Room / PDFBox / POI / ML Kit 等） |
| 分层耦合 | 34 个导航路由（32 个 Screen 文件）、16 个 ViewModel、全部 Manager/Repository/Store |
| 对外边界 | 全部出站 HTTP 调用、MCP 连接、本地子进程 |

### 1.2 对外前后端分离结论：✅ 完全分离（纯客户端）

项目 **零内嵌服务端**，所有网络成分为出站客户端请求：

| 检查项 | 结果 | 证据 |
|--------|------|------|
| HTTP 服务器 / Socket 监听 | ❌ 无 | 无 ServerSocket/NanoHTTPD 等监听代码 |
| Node / koa / express | ❌ 无 | 无 package.json/node_modules（MCP stdio 的 npx 子进程属外部命令，非内嵌服务） |
| Python 服务 | ❌ 无 | spcrk 下无 `.py`/`server/`（桌面旁 Mahjong 项目是另一独立应用） |
| 端口监听 | ❌ 无 | Manifest 无监听类权限（INTERNET/ACCESS_NETWORK_STATE 仅用于出站请求；CAMERA/通知/外部存储等为本机能力，与监听无关） |
| 外部服务调用 | ✅ 大量 | 31 家 LLM 端点、搜索引擎、MCP 服务器、Ollama 等 |

**关键判断**：App 永远是 HTTP 客户端。`localhost:11434 / :1234 / :8000`（Ollama / LM Studio / GPUStack）指向设备上独立的本地推理进程，App 只是其 HTTP 客户端，属"外部进程依赖"，非内嵌服务。

### 1.3 对内 UI↔逻辑分层结论：✅ 已重构（阶段 1~4 落地，2026-08-28）

- ✅ **全部业务对象已集中到 `AppContainer`**：repository / aiManager / searchEngine / mcpService / skillService / documentService / ocrService / localModelService / videoDownloader / settingsStore / modelConfigStore
- ✅ **16 个 ViewModel**（Chat / CodeAssistant / Translate / Document / Knowledge / LocalModel / Main / McpManage / ModelManage / Note / Ocr / Schedule / Search / SkillManage / Stats / History）经 `getAppContainer()` 取依赖
- ✅ **原 3 处架构异常全部修复**：ScheduleViewModel 不再反向引用 Activity、SkillSelectorPanel 改收 `List<SkillDisplayItem>` 数据、ModelManageViewModel 已拆独立文件
- ✅ **代码库中无 `remember { XxxManager(...) }` / `new AIManager()` / `new SearchEngine()`** 直接构造（0 匹配）
- ⚠️ 已知残留：8 个设置页在 Composable 内直取 `getAppContainer(...).settingsStore / modelConfigStore`（见 4.8 组装的说明）
- 无 DI 框架（Hilt/Koin），以手工 AppContainer 容器查询 + ViewModel Factory 注入 Application 落地

---

## 二、依赖许可证审计（30 项，编号）

### 构建插件
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 1 | `com.android.tools.build:gradle:8.1.0` | Apache 2.0 |
| 2 | `org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0` | Apache 2.0 |

### UI / 运行时
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 3 | `androidx.core:core-ktx:1.12.0` | Apache 2.0 |
| 4 | `androidx.lifecycle:lifecycle-runtime-ktx:2.6.2` | Apache 2.0 |
| 5 | `androidx.lifecycle:lifecycle-viewmodel-compose:2.6.2` | Apache 2.0 |
| 6 | `androidx.activity:activity-compose:1.8.0` | Apache 2.0 |
| 7 | `androidx.compose.ui:ui:1.5.0` | Apache 2.0 |
| 8 | `androidx.compose.ui:ui-tooling-preview:1.5.0` | Apache 2.0 |
| 9 | `androidx.compose.material:material-ripple:1.5.0` | Apache 2.0 |
| 10 | `androidx.compose.material3:material3:1.1.2` | Apache 2.0 |
| 11 | `androidx.compose.material:material-icons-extended:1.5.4` | Apache 2.0 |
| 12 | `androidx.navigation:navigation-compose:2.7.5` | Apache 2.0 |

### 网络 / 解析
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 13 | `com.squareup.okhttp3:okhttp:4.12.0` | Apache 2.0 |
| 14 | `com.google.code.gson:gson:2.10.1` | Apache 2.0 |
| 15 | `org.jsoup:jsoup:1.16.1` | MIT |

### 异步 / AI / 视频
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 16 | `org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3` | Apache 2.0 |
| 17 | `com.google.mlkit:text-recognition:16.0.0` | Apache 2.0 |
| 18 | `com.google.mlkit:text-recognition-chinese:16.0.0` | Apache 2.0 |
| 19 | `com.google.android.gms:play-services-tasks:18.1.0` | Apache 2.0 |
| 20 | `com.github.sealedtx:java-youtube-downloader:3.2.3` | Unlicense |

### 数据持久化
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 21 | `androidx.room:room-runtime:2.6.1` | Apache 2.0 |
| 22 | `androidx.room:room-ktx:2.6.1` | Apache 2.0 |
| 23 | `androidx.room:room-compiler:2.6.1` | Apache 2.0 |
| 24 | `androidx.work:work-runtime-ktx:2.9.0` | Apache 2.0 |

### 文档解析
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 25 | `org.apache.pdfbox:pdfbox:2.0.31` | Apache 2.0 |
| 26 | `org.apache.poi:poi:5.2.5` | Apache 2.0 |
| 27 | `org.apache.poi:poi-ooxml:5.2.5` | Apache 2.0 |
| 28 | `org.apache.poi:poi-scratchpad:5.2.5` | Apache 2.0 |

### Debug 专用
| 编号 | 依赖 | 许可证 |
|------|------|--------|
| 29 | `androidx.compose.ui:ui-tooling:1.5.0` | Apache 2.0 |
| 30 | `androidx.compose.ui:ui-test-manifest:1.5.0` | Apache 2.0 |

**审计结论**：全项目 30 项依赖均为 **Apache 2.0 / MIT / Unlicense**，与 Apache 2.0 开源计划 100% 兼容，无 copyleft 污染。

---

## 三、耦合屏幕重构路线图（15 个）——✅ 已完成（2026-08-28 落地）

按依赖类型分组，分 4 个阶段实施：

### 阶段 1：基础设施 — AppContainer

**目标**：消灭散落在 UI 里的 `new Manager(...)` / Application 强转。

**新建 `AppContainer`**（挂在 `VideoDownloaderApp` 上）：
```
AppContainer
├── repository（单例，替代 (app as VideoDownloaderApp).repository）
├── settingsStore（替代 SettingsStore.getInstance()）
├── modelConfigStore
├── aimanager / searchEngine / mcpManager / skillManager
├── documentManager / ocrManager / localModelManager
└── videoDownloader
```

**屏幕取用**：`(LocalContext.current.applicationContext as VideoDownloaderApp).container.xxx`

**验收**：全项目 grep 无 `remember { XxxManager(...) }` 直接构造。

> 选 AppContainer 而非 Hilt：项目无 DI、无大量单测，手工容器 30 分钟落地，Hilt 是重写。

### 阶段 2：Manager 类屏幕拆 VM（7 个，按优先级排序）

| 优先级 | 屏幕 | 新建 ViewModel | 迁移内容 |
|--------|------|----------------|----------|
| 🔴 高 | `LocalModelScreen` | `LocalModelViewModel` | 下载/导入/删除全部方法 + 下载进度 StateFlow |
| 🔴 高 | `KnowledgeScreen` | `KnowledgeViewModel` | Repository 读写 + DocumentManager |
| 🔴 高 | `McpManageScreen` | `McpManageViewModel` | McpManager 全部调用（含 parse 工具） |
| 🟡 中 | `McpSettingsScreen` | 复用 `McpManageViewModel`（未另建，两个屏幕共享同一 VM） |
| 🟡 中 | `SkillManageScreen` | `SkillManageViewModel` | SkillManager 全部 |
| 🟢 低 | `OcrScreen` | `OcrViewModel` | OcrManager（ML Kit） |
| 🟢 低 | `DocumentScreen` | `DocumentViewModel` | DocumentManager |

**每个 VM 的标准结构**：`StateFlow<UiState>` + `事件函数`；屏幕只读 State 和触发事件，不再 import 任何 ai/data 类。

### 阶段 3：架构异常修复（3 处）——✅ 已全部修复

| 异常 | 位置 | 修法 |
|------|------|------|
| VM 反向引用 Activity | `ScheduleViewModel`（原 :12） | ✅ 通知点击 Intent 的构建已移到 UI 层，VM 只返回"打开应用"意图数据 |
| VM 泄漏 Manager 给 View | `ChatViewModel`（原 :614-616） | ✅ `SkillSelectorPanel` 改为接收 `List<SkillDisplayItem>` 数据（现于 `ChatScreen.kt:336` 传参），不再传 `SkillManager` 对象 |
| VM 与屏幕同文件 | `ModelManageScreen.kt`（原 :59） | ✅ 已拆出独立 `ModelManageViewModel.kt` |

### 阶段 4：Store 类设置页轻量化（8 个）——✅ 已完成

| 屏幕 | 处理 |
|------|------|
| AppearanceSettings / DataSettings / DependenciesSettings / FileProcessingSettings / NotificationSettings / SearchSettings（6 个） | ✅ 已用 AppContainer 替换 `SettingsStore.getInstance()`；设置表单直读 StateFlow（Compose 常见模式，未建 VM） |
| ModelSettingsScreen / ProviderSettingsScreen（2 个） | ✅ `new ModelConfigStore(context)` → AppContainer 的 `modelConfigStore`；状态已抽 `ModelConfigViewModel` |

**收尾**：✅ `DataSettingsScreen.kt:15-16` 死导入已清理；每个屏幕验收 = grep 无 ai/data 包 import（数据类除外）。

---

## 四、前后端分离方案（架构重构）

### 4.0 重构目标

把当前"UI ↔ Manager 紧耦合"的散乱依赖，**重塑成 6 层单向依赖链**，核心是 **Domain Interface + Domain Model**：

```
Compose
  ↓
ViewModel
  ↓
UseCase
  ↓
Domain Interface
  ↓
Implementation
  ↓
Room   File   Network/AI/MCP
```

**核心理念**：
- **Domain Interface + Domain Model 是整套架构的重心**——UseCase 只对接口编程，不感知 Room / OkHttp / AI Manager / SQLite Entity / API JSON 任何实现细节
- **UI 完全不知道 Room / OkHttp / AIManager / API JSON / MCP / SQLite Entity**——UI 见到的是 Domain Model 或 UiModel，与传输/存储格式解耦
- **替换底层实现时，只要 Domain Contract 保持兼容，UI / ViewModel / UseCase 无需修改**；若业务能力或 Domain Contract 本身发生变化（如从文本聊天升级为流式多模态、新增工具调用、新错误模型），则只修改受影响的上层业务接口
- **AppContainer 是组装器，不是 Service Locator**——它的职责是在 Application 层创建 Implementation → UseCase → ViewModel，**UI 不主动到 AppContainer 取依赖**，依赖通过构造注入流向 UI
  - 现状（阶段 1~4）：以「容器查询」落地——ViewModel 经 `getAppContainer()` 自取依赖（`ViewModelFactory` 注入 Application），8 个设置页在 Composable 内直取 `settingsStore/modelConfigStore`
  - 目标（本章）：迁移到「构造注入」——组合根在各屏幕入口 `viewModel(factory = XxxViewModelFactory(container))` 捕获容器，逐步移除 VM 内部 `getAppContainer()`；注入机制见 4.8

### 4.1 六层职责边界

| 层 | 包路径（建议） | 职责 | 依赖 | 禁止 |
|----|---------------|------|------|------|
| **UI（Compose）** | `ui/` | 屏幕布局、动画、主题、导航；订阅 `StateFlow<UiState>` 渲染 | ViewModel | 直接访问 Room、OkHttp、AI Manager、API JSON、SQLite Entity、MCP |
| **ViewModel** | `ui/*ViewModel.kt` | 持有 `StateFlow<UiState>`；接收 UI 事件、调用 UseCase；**不持有任何 Repository/Manager 实例**，只持有 UseCase | UseCase | 直接访问 Room、OkHttp、Repository；通过 getter 暴露 Manager 给 View |
| **UseCase** | `domain/usecase/`（新增） | 单一业务操作（SendMessage / DownloadVideo / ListMcpTools 等）；组合 1~N 个 Repository；输入校验、业务规则；**纯 Kotlin** | Repository Interface + Domain Model | 持有 ViewModel 引用；import Compose |
| **Domain Interface** | `domain/repository/`、`domain/model/`（新增） | **架构核心**：定义 `XxxRepository` 接口 + `XxxModel` data class；接口的入参/出参全部是 Domain Model，不暴露 Entity、JSON、Cursor 等实现细节 | Kotlin Stdlib、kotlinx.coroutines（suspend / Flow） | 任何 Room / OkHttp / Android SDK / API DTO |
| **Implementation** | `data/repository/`（新增）、`ai/`、`downloader/` | 实现 `Domain Interface`；把 Room Entity ↔ Domain Model 互转、把 JSON ↔ Domain Model 互转、调用 OkHttp/AI Manager 拿到数据后映射回 Domain Model | Domain Interface、Room、OkHttp、AI Manager、JSON | 被 ViewModel/UseCase 直接调用（只能通过 Interface） |
| **Infrastructure** | `core/db/`（Room）、`core/network/`（OkHttp）、`ai/`（Manager）、`downloader/` | Room Database / OkHttp Client / AI Manager / Search Engine / MCP Manager / Video Downloader / OCR / Document 等"原子能力"；无业务组合 | Android SDK、第三方库 | 引用 Domain Model 之外的任何上层类型 |

### 4.2 Domain Model 与 UiModel 边界

| 类型 | 定义 | 示例 | 可见范围 |
|------|------|------|----------|
| **Domain Model** | 业务核心数据，跨层共享 | `MessageModel(val id: String, val content: String, val createdAt: Instant)` | UI、ViewModel、UseCase、Repository、Implementation |
| **UiModel** | UI 专属展示状态，含 UI 行为标记 | `ChatMessageUi(val isExpanded: Boolean, val isStreaming: Boolean, val formattedTime: String, val isError: Boolean)` | UI 层内部（Compose 内转换，不跨层） |

**规则**：
- Domain Model 是纯 Kotlin data class，无平台依赖
- UiModel 在 Compose 内由 Domain Model + 交互状态组合生成（如 `message.toUiModel(isExpanded, isStreaming)`），**不进入 ViewModel 以下各层**
- ViewModel 暴露 `StateFlow<UiState>`，UiState 内同时持有 Domain Model 列表和 UI 状态（如 `val messages: List<MessageModel>, val isStreaming: Boolean`）

### 4.3 逐步淘汰"巨型 Manager" → 明确 DataSource

**当前问题**：`AIManager`、`McpManager`、`SearchEngine` 等类承担了"组合多个数据源 + 业务逻辑"双重职责，长期会演变为新的 God Object。

**目标命名**（✅ 5/7 已接口化——5 个 Manager 已实现 `ai/api/*Service` 接口；剩余 2 个见下）：

| 当前 | 建议 | 状态 |
|------|------|------|
| `AIManager` | `OpenAiDataSource` | ⏳ 待建接口（仿 `McpService`）+ 迁 `data/` |
| `SearchEngine` | `SearchDataSource` | ⏳ 待建接口 + 迁 `data/` |
| `McpManager` | `McpDataSource` | ✅ 已实现 `McpService`（`ai/api/McpService.kt`） |
| `SkillManager` | `SkillDataSource` | ✅ 已实现 `SkillService` |
| `VideoDownloader` | `VideoDataSource` | ✅ 已进 AppContainer（`videoDownloader`） |
| `DocumentManager` | `DocumentDataSource` | ✅ 已实现 `DocumentService` |
| `OcrManager` | `OcrDataSource` | ✅ 已实现 `OcrService` |

**Repository 负责组合**（业务逻辑留在 Domain 层，不在 DataSource 内部）：
```
ChatRepositoryImpl
    ├── OpenAiDataSource
    ├── McpDataSource
    └── SkillDataSource
```

### 4.4 长任务架构（下载 / WorkManager）

下载类长任务（视频下载、大模型下载、批量文档导入）**不能由 ViewModel 直接管理**——配置变更或进程重建后会丢失。

**目标分层**：

```
DownloadUseCase（Domain 层，组合 DownloadRepository）
      ↓
DownloadRepository（Domain Interface）
      ↓
DownloadWorker（Infrastructure，WorkManager）
      ↓
VideoDataSource / ModelDataSource（原子下载能力）
```

**规则**：
- ViewModel 只调用 `DownloadUseCase.start(url): Flow<DownloadProgress>`，不直接持有 `VideoDownloader`
- `DownloadWorker` 由 WorkManager 调度，进程重建后自动恢复
- 下载进度通过 `DownloadRepository.getProgress(id): Flow<DomainModel>` 暴露给上层
- UI 取消下载 = `DownloadUseCase.cancel(id)`，不直接操作 Worker

### 4.5 重塑后的依赖流向图

```
┌──────────────────────────────────────────────────────┐
│                    UI（Compose）                       │
│         只见 Domain Model，不知 Room/Json/MCP         │
└────────────────────┬─────────────────────────────────┘
                     │ subscribe StateFlow<UiState>
                     ▼
┌──────────────────────────────────────────────────────┐
│                       ViewModel                        │
│         只见 UseCase 暴露的 suspend 函数               │
└────────────────────┬─────────────────────────────────┘
                     │ invoke()
                     ▼
┌──────────────────────────────────────────────────────┐
│                       UseCase                         │
│     只见 Repository Interface + Domain Model           │
│     （SendMessage、ParseVideo、ListMcpTools ...）       │
└────────────────────┬─────────────────────────────────┘
                     │ interface
                     ▼
┌──────────────────────────────────────────────────────┐
│          Domain Interface + Domain Model              │
│      ★ 架构核心 ★                                         │
│   interface ChatRepository {                            │
│     suspend fun send(msg: MessageModel): Flow<MessageModel> │
│   }                                                     │
│   data class MessageModel(... val createdAt: Instant)  │
└────────────────────┬─────────────────────────────────┘
                     │ implements
                     ▼
┌──────────────────────────────────────────────────────┐
│              Implementation（适配层）                  │
│   RoomChatRepository / OkHttpChatRepository / ...      │
│   Entity ↔ Model   JSON ↔ Model   Cursor ↔ Flow<Model> │
└────────────────────┬─────────────────────────────────┘
                     │ uses
                     ▼
┌──────────────────────────────────────────────────────┐
│   Room  │  File  │  Network/AI/MCP  │  Document  │ ... │
│   （原子能力，无业务组合）                                  │
└──────────────────────────────────────────────────────┘
```

### 4.6 现在 vs 未来对比

**现在**（UI 直接见到底层实现）：
```
Room
OkHttp
AIManager
SearchEngine
McpManager
Server/API     ← 全部直接暴露给 UI，UI 直接 import
```

**未来**（UI 只见到 Domain Interface + Model）：
```
UI 不知道 Room
UI 不知道 OkHttp
UI 不知道 AIManager
UI 不知道 API JSON
UI 不知道 MCP
UI 不知道 SQLite Entity
```

**未来收益**：
- 切换 Room → SQLDelight：只改 Implementation
- 切换 OkHttp → Ktor：只改 Implementation
- 把 Ollama 换成自建后端：只改 Implementation
- JSON 改 Protobuf：只改 Implementation
- **只要 Domain Contract 保持兼容，UI / ViewModel / UseCase 无需修改**；若业务能力或 Domain Contract 本身发生变化（如从文本聊天升级为流式多模态、新增工具调用、新错误模型），则只修改受影响的上层业务接口

### 4.7 关键约束规则（强制）

| # | 规则 | 违规检测命令 |
|---|------|--------------|
| 1 | **UI 只见 Domain Model**：禁止 UI 层 import `com.spcrk.app.data.*`、`com.spcrk.app.ai.*`（除纯 data class 转发） | `grep -r "import com.spcrk.app.data\.\|import com.spcrk.app.ai\." app/src/main/java/com/spcrk/app/ui/` |
| 2 | **ViewModel 只见 UseCase**：禁止 import Repository 接口之外的任何实现；禁止通过 getter 暴露 Manager/Repository 给 View（含 `getConnectionState`/`getToolsForServer`/`callTool`/`parse*` 等透传方法） | `grep -r "fun get.*(Manager\|Repository)\|fun (call\|parse\|connect).*(" app/src/main/java/com/spcrk/app/ui/` |
| 3 | **UseCase 只见 Interface**：禁止 UseCase 出现 import Room、OkHttp、JSON、Compose（⚠️ 规则 3/4/6/7 在 `domain/` 建立（M0）后才生效） | `grep -r "import androidx.compose\|import androidx.room\|import okhttp3" app/src/main/java/com/spcrk/app/domain/usecase/` |
| 4 | **Domain Interface 纯净**：interface 文件只依赖 Kotlin stdlib + 协程，零平台依赖 | `grep -r "import" app/src/main/java/com/spcrk/app/domain/` 仅显示 model + repository 文件 |
| 5 | **Manager 不跨层调用**：Manager 之间禁止互相持有；需要组合时由 UseCase 在 Domain 层组合（锚点为接口/容器，而非旧类名 `McpManager()` 等） | `grep -r "AIManager()\|SearchEngine()\|McpManager()" app/src/main/java/com/spcrk/app/ai/` 仅 Implementation 层，且 VM/Manager 一律走 `container.*` |
| 6 | **Entity 不外泄**：Room Entity / API JSON DTO 严格停留在 Implementation 层内部，外层只见 Domain Model | `grep -r "import com.spcrk.app.data" app/src/main/java/com/spcrk/app/domain/` |
| 7 | **Domain 层不得 import android.\***：Domain Interface / UseCase / Domain Model 禁止引用 Android SDK | `grep -r "import android\." app/src/main/java/com/spcrk/app/domain/` |
| 8 | **Implementation 不得反向 import ui.\***：Implementation 层禁止引用 UI 层类型 | `grep -r "import com.spcrk.app.ui" app/src/main/java/com/spcrk/app/data/` |
| 9 | **UI / ViewModel 不得直接创建 Repository / Manager / Service**：禁止 `remember { XxxManager(...) }`、`private val x = XxxManager()`（无 remember 的属性初始化同样违规） | `grep -r "remember.*\(Repository\|UseCase\|Manager\|Service\)\|= \(AIManager\|SearchEngine\|McpManager\|SkillManager\|DocumentManager\|OcrManager\|LocalModelManager\|VideoDownloader\|Repository\)(" app/src/main/java/com/spcrk/app/ui/ app/src/main/java/com/spcrk/app/ai/` |

### 4.8 实施步骤（按"自底向上"顺序）

| 步骤 | 内容 | 涉及文件 | 工作量 |
|------|------|----------|--------|
| ⓪ M0 | 建立 `domain/` 骨架 + 首批 Domain Model（`MessageModel`、`VideoModel`、`McpToolModel` 等）——**后续步骤与验收规则的前置门槛** | 新建 `domain/model/*.kt` | 0.5 天 |
| ① 定义 Domain Model | 把现有 `Note`、`ChatMessage`、`DownloadHistory` 等 data class 提升为 Domain Model | 新建 `domain/model/*.kt` | 1 天 |
| ② 定义 Domain Interface | 为每类业务定义 Repository Interface（`ChatRepository`、`McpRepository`、`VideoRepository`、`KnowledgeRepository`、`SettingsRepository`） | 新建 `domain/repository/*.kt` | 1.5 天 |
| ③ 写 UseCase | 每个屏幕 1~3 个 UseCase（SendMessageUseCase、ParseVideoUrlUseCase、ListMcpToolsUseCase 等）；组合多个 Repository 实现完整业务 | 新建 `domain/usecase/*.kt` | 2 天 |
| ④ 写 Implementation | 把现有 Repository / Manager 改造为 `XxxRepositoryImpl`，实现对应 Interface；Entity ↔ Model / JSON ↔ Model 映射 | 改造 `data/`、`ai/` | 3 天 |
| ⑤ 改 ViewModel | ViewModel 改为注入 UseCase；状态从 `StateFlow<UiState>` 暴露 Domain Model | 改造所有 ViewModel | 1.5 天 |
| ⑥ 改 UI | 屏幕订阅 `StateFlow<UiState>` 渲染 Domain Model；移除所有 ai/data 包 import（除纯 model） | 改造所有 Composable 屏幕 | 1.5 天 |
| ⑦ 写单测 | 为 UseCase 和 Implementation 写单元测试（mock Interface、Room 内存数据库、MockWebServer） | 新建 `test/` | 2 天 |

**组装机制（组合根）**：
- 组合根设在各屏幕入口：`viewModel(factory = XxxViewModelFactory(container))`，**容器只在组合根被捕获一次**，之后依赖沿构造注入向下传递，VM 内部逐步移除 `getAppContainer()`
- 过渡期（现状）允许 VM 经 `getAppContainer()` 自取依赖，但**设置页 Composable 内的直取（8 处）是明确的清理目标**，随 M1（SettingsRepository）迁移一并消除
- 不引入 Hilt/Koin：项目无 DI 基础设施，手工组合根可逐域替换、风险可控，避免一次重写

**总计**：约 **13 个工作日**（含单测）；不含单测约 **11 天**。

### 4.9 增量交付策略

不重写全部，按业务域**一个一个迁移**，每完成一个域立即获得收益：

| 里程碑 | 迁移内容 | 收益 | 状态 |
|--------|----------|------|------|
| M1 | SettingsRepository（轻量、模型已存在） | 演示完整链路，6 层架构雏形 | ⏳ 未开始 |
| M2 | NoteRepository + NoteViewModel + NoteEditScreen | 第一个端到端落地的屏幕 | ⏳ 未开始（Note 功能已存在，仅需接口化迁移） |
| M3 | DownloadHistoryRepository + HistoryScreen | 数据类多但 Repository 简单 | ⏳ 未开始（History 功能已存在，仅需接口化迁移） |
| M4 | KnowledgeRepository + KnowledgeScreen | 引入 DocumentManager 的双 Model 映射 | ⏳ 未开始 |
| M5 | ChatRepository（最复杂：流式 SSE + MCP + Skill） | 核心价值流 | ⏳ 未开始（ChatViewModel 已收敛，迁移成本已降低） |
| M6 | McpRepository / VideoRepository / 其余 | 收尾 | ⏳ 未开始 |

每个 M 完成后 6 层依赖都更清晰，UI/ViewModel 对底层的耦合都减少，**渐进式收益**而非"一次性大爆炸"。

---

## 五、重构验收标准

### 5.1 分层合规检查（grep 验证）

> 以下为 **M0~M6 完成后**的目标状态。当前阶段 1~4 已达成部分（下表"现状"列）。注意：3 个 Screen + 3 个 ViewModel 位于 `ai/` 包下（ChatScreen/TranslateScreen/CodeAssistantScreen 及对应 VM），属 UI 层，验收时需一并扫描 `ai/`（排除 `*Screen.kt` 与 `ai/api/*Service` 接口转发）。

| 检查项 | 命令 | 预期结果 | 现状 |
|--------|------|----------|------|
| UI 层无业务 import | `grep -r "import com.spcrk.app.ai" app/src/main/java/com/spcrk/app/ui/` + `grep -r "import com.spcrk.app.ai" app/src/main/java/com/spcrk/app/ai/*Screen.kt`（仅允许 `ai.api.*` 接口与纯数据类） | 0 匹配（M6 后） | 过渡态：仅 `ai.api.*` 接口 + `SearchConfig`/`PresetModels`/`providerBaseUrls` 等数据转发 |
| UI 层无数据 import | `grep -r "import com.spcrk.app.data" app/src/main/java/com/spcrk/app/ui/` | 0 匹配（M6 后，data class 除外） | 过渡态：import 多为 data class / `Repository` / `SettingsStore` |
| ViewModel 无 Manager 泄漏 | `grep -r "fun get.*Manager\|fun (call\|parse\|connect).*(" app/src/main/java/com/spcrk/app/ui/` | 0 匹配（M6 后） | 过渡态：6 处匹配——`McpManageViewModel` 4 处 Service 透传（`callTool`/`parseDxtFile`/`parseMcpbFile`/`parseJsonConfig`，待 UseCase 层接管）；`ScheduleScreen`/`ScheduleViewModel` 2 处纯 cron 字符串解析（误报） |
| UseCase 无 Compose | `grep -r "import androidx.compose" app/src/main/java/com/spcrk/app/domain/` | 0 匹配（M0 建立 `domain/` 后生效） | ⏳ `domain/` 未建立 |
| Manager 无 View 引用 | `grep -r "import androidx.compose\|import com.spcrk.app.ui" app/src/main/java/com/spcrk/app/ai/`（排除 `*Screen.kt`） | 0 匹配 | ✅ 已达成（仅 `*Screen.kt` 属 UI 层，属正常） |

### 5.2 编译与功能验收

- ✅ `assembleDebug` 编译通过
- ✅ 全部 34 个导航路由（32 个 Screen 文件）正常渲染、导航、状态保持
- ✅ AI 对话（OpenAI 兼容 / Ollama / MCP）功能正常
- ✅ 视频下载、文档处理、OCR、知识库功能正常
- ✅ 设置页读写、主题切换、数据管理功能正常
- ✅ 全部单元测试通过（当前为 Manager/ViewModel/Processor 层测试；`domain/` 建立（M0）后新增 UseCase 层测试，覆盖率目标 > 80%）

---

## 六、实施优先级与时间估算

| 阶段 | 内容 | 工作量 | 风险 |
|------|------|--------|------|
| 1 | Domain Model + Domain Interface 定义 | 3 天 | 低 |
| 2 | UseCase 层编写 | 2 天 | 中 |
| 3 | Implementation 层（DataSource + RepositoryImpl） | 3 天 | 中 |
| 4 | ViewModel 改造（构造注入 UseCase） | 1.5 天 | 中 |
| 5 | UI 层清理（移除 ai/data import） | 1.5 天 | 低 |
| 6 | 单测（UseCase + Implementation） | 2 天 | 低 |

**总计**：约 **13 个工作日**（含单测）；不含单测约 **11 天**。

---

## 七、附录：外部服务依赖清单

| 类型 | 端点 | 文件位置 |
|------|------|----------|
| LLM（OpenAI 兼容） | `https://api.openai.com/v1` 等 31 家 | `ai/SearchEngine.kt:55-87`（`providerBaseUrls`）、`ai/AIManager.kt:85-166`（请求构造） |
| 本地 LLM | `http://localhost:11434/v1`（Ollama）等 | `ai/SearchEngine.kt:84-86` |
| 搜索 | DuckDuckGo / Bing / Tavily / SearXNG / Exa / Bocha / Jina / Firecrawl | `ai/SearchEngine.kt:197-729`（搜索实现区） |
| MCP | 用户配置的 SSE / HTTP / stdio | `ai/McpManager.kt:74-297` |
| 技能 | `https://wttr.in/{city}?format=j1` 等 | `ai/SkillManager.kt:126-194` |
| 文档 | 本地处理（无网络） | `ai/DocumentManager.kt`、`ai/OcrManager.kt` |
| 视频 | java-youtube-downloader 库内部 | `downloader/VideoDownloader.kt`、`downloader/VideoParser.kt` |
